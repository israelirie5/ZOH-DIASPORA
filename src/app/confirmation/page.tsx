import Link from "next/link";
export default async function Confirmation({searchParams}:{searchParams:Promise<{token?:string}>}) { const {token}=await searchParams; return <main className="grid min-h-screen place-items-center bg-[#073f36] p-5"><section className="max-w-xl bg-white p-8 text-center text-[#073f36]"><p className="font-black text-[#176652]">INSCRIPTION CONFIRMÉE</p><h1 className="mt-3 text-3xl font-black">Votre inscription a bien été enregistrée.</h1><p className="my-5 text-slate-600">Votre ticket vient d’être envoyé par email.</p>{token&&<Link className="inline-flex min-h-14 items-center bg-[#ffd21c] px-6 font-black" href={`/ticket/${token}`}>AFFICHER MON TICKET</Link>}</section></main> }
